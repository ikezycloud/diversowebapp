version: "3"
services:
  diversowebapp:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: diversowebapp
    ports:
      - "8001:8001"
    volumes:
      - .:/diverso-app
